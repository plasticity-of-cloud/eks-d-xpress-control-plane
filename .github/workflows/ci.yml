name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build-and-test:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v5

      - uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: '21'
          cache: maven

      - name: Build and test
        run: mvn -B verify -DskipITs

      - name: Build container images (JVM)
        run: |
          mvn -B -pl eks-pod-identity-crd,eks-auth-proxy package -DskipTests \
            -Dquarkus.container-image.build=true \
            -Dquarkus.container-image.push=false
          mvn -B -pl eks-pod-identity-crd,eks-pod-identity-webhook package -DskipTests \
            -Dquarkus.container-image.build=true \
            -Dquarkus.container-image.push=false

      - name: Lint Helm charts
        run: |
          helm lint eks-auth-proxy/target/helm/kubernetes/eks-auth-proxy
          helm lint eks-pod-identity-webhook/target/helm/kubernetes/eks-pod-identity-webhook
