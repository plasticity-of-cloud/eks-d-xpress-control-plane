name: Dependabot Auto-merge

on:
  pull_request:
    branches: [main]

jobs:
  # Auto-merge disabled — Dependabot PRs require manual review
  auto-merge:
    runs-on: ubuntu-24.04
    if: false
    steps:
      - run: echo "disabled"
